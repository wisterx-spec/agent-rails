# .slimignore — 项目瘦身豁免清单
#
# 用途：声明不参与 /slim 工作流孤儿扫描的路径和模式
# 规则：
#   - 每行一条路径或 glob 模式
#   - 支持 # 注释
#   - 相对于项目根目录
#
# 何时需要加这里：
#   1. 动态路由/懒加载组件（路径由运行时字符串拼接）
#   2. 插件/扩展点注册目录（框架运行时自动扫描）
#   3. 静态分析不可见的反射调用目标
#   4. 曾经被 /slim 误删后恢复的文件（加注释说明原因）
#
# ⚠️  不要把"懒得排查"的文件加进来，这会降低瘦身效果

# 示例：动态加载的路由页面目录
# src/pages/dynamic/

# 示例：框架自动扫描的插件目录
# src/plugins/

# 示例：曾被误删，实际有动态引用
# src/components/LazyRenderer.tsx  # 误删于 2024-03-01，被 loadComponent('LazyRenderer') 动态引用

# 示例：测试 fixture 文件（无 import 但被测试框架自动加载）
# src/__fixtures__/

# 在此添加项目专属豁免规则 ↓
